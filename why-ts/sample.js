// const sum = (a, b) => {
//   return a + b;
// };

// sum(10, '20');
/**
 *
 * @param {number} a first number
 * @param {number} b second number
 * @returns
 */
function sum(a, b) {
  return a + b;
}
sum(10, 20);
